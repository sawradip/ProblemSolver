#include<iostream>

using namespace std;

int lcs( int i, int j, string s1, string s2, vector<vector<int>> &t){

    if ( i >= s1.size() || j >= s2.size()){
        return t[i][j]
    }

    if
}

int main(){

    string s1 = "donet";
    string s2 = "none";

    int i = 0; j = 0;
    vector<vector<int>> t;
    cout << lcs( i, j, s1, s2, t);

}
